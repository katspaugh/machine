# Template for git's ssh-signing allowed_signers file. Rendered by bin/machine
# (render_git_templates) and pushed to ~/.config/git/allowed_signers inside the
# VM. The git config below references it via gpg.ssh.allowedSignersFile.
__GIT_EMAIL__ __GIT_SIGNING_KEY__
